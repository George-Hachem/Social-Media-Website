<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Instagram Profile Page</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
    }
    .profile-header {
      background-color: #f0f0f0;
      padding: 20px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .profile-header .profile-avatar {
      width: 100px;
      height: 100px;
      border-radius: 50%;
      background-color: #ccc;
    }
    .profile-header .profile-info {
      flex-grow: 1;
      margin-left: 20px;
    }
    .profile-header .profile-info h1 {
      margin: 0;
      font-size: 24px;
    }
    .profile-header .profile-info p {
      margin: 5px 0 0 0;
      font-size: 16px;
    }
    .profile-posts {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      grid-gap: 10px;
      padding: 20px;
    }
    .profile-posts .post-item {
      background-color: #f0f0f0;
      padding: 10px;
      display: flex;
      justify-content: center;
      align-items: center;
    }
    .profile-posts .post-item img {
      max-width: 100%;
      max-height: 100%;
    }
  </style>
</head>
<body>
<form action="/profile" method = "POST">
  <div class="profile-header">
    <div class="profile-avatar"></div>
    <div class="profile-info">
      <h1><%= userProfile %></h1>
      <p>Bio</p>
      <% if (doesFollow) { %> 
        <!-- if doesFollow is null -->
        <input type="submit" name = "unfollow" value = "unfollow">
      <% } else{ %>
        <input type="submit" name = "follow" value = "follow">
      <% } %>
    </div>
    <p> followers: <%= followerCount %></p>
    <p> following:  <%= followCount %></p>
    <button>Edit Profile</button>
  </div>
  <div class="profile-posts">
    <div class="post-item">
      <img src="path_to_image.jpg" alt="Post 1">
    </div>
    <div class="post-item">
      <img src="path_to_image.jpg" alt="Post 2">
    </div>
    <div class="post-item">
      <img src="path_to_image.jpg" alt="Post 3">
    </div>
    <!-- Add more post items as needed -->
  </div>
</form>
</body>
</html>